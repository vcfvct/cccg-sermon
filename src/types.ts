export interface PodCastMeta {
  id: number;
  title: string;
  body: string;
  author: string;
  eventDate: string;
  eventName: string;
  audio?: string;
  video?: string;
  youtubeUrl?: string;
  description?: string;
}
